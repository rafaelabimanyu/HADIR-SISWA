/* General Styles */
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #2e2e2e; /* Dark background */
            color: #e0e0e0; /* Light gray text */
        }

        html {
            scroll-behavior: smooth;
        }

        header {
            background: #1f1f1f; /* Darker background for header */
            color: #e0e0e0;
            padding: 10px 0;
        }

        nav ul {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
        }

        nav ul li {
            margin: 0 15px;
        }

        nav ul li a {
            color: #e0e0e0;
            text-decoration: none;
            font-size: 18px;
            padding: 5px 10px; /* Padding for box effect */
            border-radius: 3px; /* Slightly rounded corners */
            border: 1px solid #e0e0e0; /* Light border for box effect */
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        nav ul li a:hover {
            background-color: #444; /* Darker gray for hover effect */
            color: #fff;
        }

        .hero {
            background: linear-gradient(135deg, #444, #555, #666); /* Gradient background */
            color: #fff;
            text-align: center;
            padding: 100px 20px;
        }

        .hero h1 {
            font-size: 48px;
            margin-bottom: 20px;
        }

        .hero p {
            font-size: 24px;
            margin-bottom: 40px;
        }

        .cta-button {
            background: #666; /* Medium dark gray */
            color: #fff;
            padding: 10px 20px;
            border-radius: 25px;
            text-decoration: none;
            font-size: 18px;
            transition: background-color 0.3s ease;
        }

        .cta-button:hover {
            background-color: #777; /* Slightly lighter gray */
        }

        .info {
            display: flex;
            justify-content: center;
            margin: 50px 20px;
            flex-wrap: wrap;
        }

        .info-box {
            background: #333; /* Dark background */
            padding: 20px;
            margin: 10px;
            border-radius: 10px;
            text-align: center;
            flex: 1;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            max-width: 300px;
            color: #e0e0e0; /* Light gray text */
            box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.3); /* Darker shadow for contrast */
        }

        .info-box:hover {
            transform: translateY(-10px);
            box-shadow: 0px 10px 15px rgba(0, 0, 0, 0.5); /* Darker shadow on hover */
        }

        footer {
            background: #1f1f1f; /* Darker background for footer */
            color: #e0e0e0;
            text-align: center;
            padding: 10px 0;
        }

        /* Modal */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.7);
            justify-content: center;
            align-items: center;
        }

        .modal-content {
            background-color: #333; /* Dark background for modal */
            padding: 20px;
            border-radius: 10px;
            max-width: 500px;
            width: 100%;
            text-align: center;
            position: relative;
            color: #e0e0e0; /* Light gray text */
        }

        .close {
            position: absolute;
            top: 10px;
            right: 20px;
            font-size: 24px;
            cursor: pointer;
        }

        .article {
            margin: 50px 20px;
            padding: 20px;
            background-color: #444; /* Dark background */
            border-radius: 10px;
            box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.3);
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }

        .article h2 {
            color: #fff; /* Light text color for headings */
            margin-bottom: 15px;
        }

        .article p {
            color: #ccc; /* Lighter gray for text */
            line-height: 1.6;
        }